<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.marsev">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SalesActivity"></activity>
        <activity android:name=".ShopFrontForCustomerActivity" />
        <activity android:name=".AddAnItemActivity" />
        <activity android:name=".ShopFrontActivity" />
        <activity android:name=".ViewYourShopsActivity" />
        <activity android:name=".EditUserActivity" />
        <activity android:name=".StartAShopActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".CheckOut" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".Register" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>